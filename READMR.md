# 🤖 بوت الآلة الحاسبة الذكي (Telebot Calculator)

بوت تلجرام متطور يقوم بإجراء العمليات الحسابية مباشرة من خلال الدردشة، تم تطويره باستخدام مكتبة `pyTelegramBotAPI`.

## 🚀 المميزات
* **ذكاء اصطناعي بسيط:** يفهم العمليات الحسابية المكتوبة في رسالة واحدة (مثل `5+5`).
* **دعم الرموز العربية:** يتعامل مع رموز الضرب `×` والقسمة `÷` والرموز البرمجية `*` و `/`.
* **معالجة الأخطاء:** يحذرك عند محاولة القسمة على صفر أو إدخال نص غير مفهوم.
* **تنسيق تلقائي:** يظهر النتائج بشكل نظيف (مثلاً `10` بدلاً من `10.0`).

## 🛠 المتطلبات البرمجية
يجب أن يكون لديك Python مثبت على جهازك، بالإضافة إلى المكتبة التالية:
```bash
pip install pyTelegramBotAPI
